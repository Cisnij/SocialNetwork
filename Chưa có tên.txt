
chưa xong
-sửa lại js phần bạn
-thiếu phần đăng nhập:, xác thực otp khi bật 2 bước, xóa tài khoản
- tạo group
- thêm vào reaction cho message, thêm vào setting cho chat, có role các kiểu bên trong model conversationmember,
- api tạo group, api thêm member, thêm quyền vào db conversationmember is_admin=true, tạo api thêm quyền cho user trong đoạn chat, thêm avatar, group name vào db conversation
- xong tất cả thì xây signal 1 thể
- xây chức năng lên lich tạo post dùng thư viện schedule và redis và hiện calender lên lịch



-thêm throttle,signal
-thêm setting default post privacy
-thêm story 24h
-tạo group và phân quyền chỉnh sửa bài viết trong group.
-xây group chat

- thêm 1 cái là upload video và thêm model PostVideo luôn làm y chang PostPhoto
-dùng celery(dễ), mai check các api nào cần dùng và test, trước là gửi otp 
    Push Firebase notification — đang gọi trực tiếp trong WebSocket consumer, chậm và block
    Gửi email — verify email, OTP, cảnh báo đổi email
    Elasticsearch sync — index bài viết/profile sau khi save
    Xóa dữ liệu cũ — token hết hạn, OTP cache, soft delete cũ
    # terminal 2 — worker cho notification
    celery -A backend worker -Q notifications --loglevel=info

    # terminal 3 — worker cho email
    celery -A backend worker -Q emails --loglevel=info

    # terminal 4 — beat scheduler
    celery -A backend beat --loglevel=info --scheduler django_celery_beat.schedulers:DatabaseSchedule


4 api mới của
    search history
    pin post
    report
    ws typing
    private/birth sdt profile
    update fe nếu thấy is_complete=False thì hiện banner hoàn thiện profile(picture) của bạn ở trang của bạn, user sửa thông tin thì be tự đánh is_complete=True
    xem tất cả các api được gọi ở 1 trang xem có thừa, test đủ các api và fe đã dùng đủ chức năng chưa và đúng logic chưa
    websocket fe bị lỗi, giải thích AI là có flow ping pong
    thêm luồng otp cho email 1 api, khi gửi lại otp fe phải có countdown tránh spam
    thêm trang support để user gửi ticket lên






















































































