docker, redis, git actions, aws, render(ưu tiên trước aws)


tích hợp đăng post,post article, chat realtime, ecommerce, video web

deploy tất cả đều cùng 1 server :
FE: socialnetwork.com (server public)
BE api.socialnetwork.com (server public)
"redis://127.0.0.1:6379" (server private) 127.0.0.1 là tại đó, nếu là máy bạn thì là local, nếu là server thì là server
elastic: "http://127.0.0.1:9200" (server private)

1 server có nhiều worker để khi worker chạy hết thì chuyển sang worker khác gọi là multi worker,tăng rq/s nhưng tăng ram, nếu full tất cả worker thì nhảy qua server khác gọi là load balancer


file python nên chia nhỏ r như sau
app/user
app/notification
app/post
app/ comment
trong mỗi phần app/ đó sẽ có model, serializer, và file service viết logic chung(bussiness logic vào) để tái sử dụng
---


1. **Đăng nhập & Xác thực**

✅ Đăng nhập, đăng xuất 
✅Quên mật khẩu
✅Đặt lại mật khẩu
✅ Xác thực Gmail / Google / Facebook
✅ Xác thực email
✅ Kết nối tài khoản xã hội
✅ Giới hạn đăng nhập (chống spam, brute force)
✅chống spam, chống đăng nhập nhiều lần, lưu token
* CAPTCHA
* Cloudflare bảo vệ
✅ Đăng ký có xác minh email

✅ Xác nhận email → tạo profile

✅ Đăng nhập (username/password)

✅ Đăng nhập bằng Google OAuth

✅ Quên mật khẩu / reset password qua email

✅ Access + Refresh token (JWT)

✅ Refresh tự động và lưu localStorage

#### 2. **Bài viết (Post)**

✅Tạo bài viết có upload nhiều ảnh
✅ Hiển thị đẹp trên trang chủ
✅Pagination + infinite scroll
✅ Phân quyền chỉnh sửa bài viết
✅ Cập nhật bài viết (nếu có quyền)
* Chế độ công khai / riêng tư
* Lưu bài viết
* khôi phục bài viết

#### 3. **Bài viết học thuật (Post Article)**

* Bài viết dài \~2000 chữ
* Có thể chèn nhiều ảnh minh họa
* Giao diện đẹp (tách ra file HTML dùng Bootstrap5 nếu nhiều ảnh)
* Xuất PDF

#### 4. **Chat Realtime**

✅ Tin nhắn thời gian thực (WebSocket)
✅ Read/Unread
* Chat nhóm
* Gửi ảnh trong chat
✅ Online/offline
#### 5. **Kết bạn**

✅Gửi lời mời kết bạn
* Gợi ý bạn bè
✅Tạo kết nối WebSocket cho bạn bè

#### 6. **Thông báo**

✅ Gửi thông báo realtime

#### 7. **Tối ưu hệ thống**

✅ Tối ưu truy vấn DB
✅ Ghi lại lịch sử hoạt động (like, comment, view, ...)

#### 8. **Dịch nội dung**

* Áp dụng API dịch (ví dụ Google Translate API)

#### 9. **Like & Bình luận**

✅ Like và bình luận realtime
* Bình luận đa tầng (nested comment)

#### 10. **Xử lý ảnh**

* Thumbnail ảnh, crop, resize nâng cao

#### 11. **Cài đặt giao diện & tài khoản**

* Chuyển sáng/tối
✅ Xem lịch sử hoạt động (like, comment,…)
✅ Xoá tài khoản

#### 12. **Trang cá nhân**

✅Hiển thị các bài viết của người dùng

#### 13. **API backend**

✅ Xây dựng API hoàn chỉnh

#### 14. **Triển khai**

* Deploy lên Render

#### 15. **Chatbot**

* Tích hợp API chatbot

#### 16. **Thống kê**

✅ Đếm lượt view, like, truy cập
* Dashboard hiển thị dữ liệu

#### 17. **Tìm kiếm**

* Tìm bài viết
✅ Tìm bạn bè

#### 18. **Báo cáo bài viết**

* Gửi report bài viết sai phạm

#### 19. **Lên lịch đăng bài**

* Đặt lịch cho bài viết

#### 20. **Chống mã độc**

✅Ngăn mã độc/chèn script độc hại

#### 21. **Thanh toán**

* Tích hợp thanh toán

#### 22. **Phát triển Mobile**

* Học Node.js và triển khai cho mobile (tương lai)


=================================================================================================================
logic tạo group và phân quyền chỉnh sửa bài viết trong group

group > user
group > post, trường is_accept=False, tạo api khi nhấn đồng ý thì truyền vào id bài và accept=True và 1 api hủy để delete

 ví dụ group 1 có user a và user b
 ví dụ group 1 có 2 bài post a và b
thì groupuser có field is_admin
và khi list all post thì sẽ liệt kê user có trong group đó không, user có phải là admin không nếu không thì chỉ return, nếu có thì liệt kê ra hết post, serializer có field là is_admin để fe hiểu là đc chỉnh sửa
tạo view cho list all post cho user riêng dùng generics List
tạo view cho list và sửa cho admin dùng generic retrieveupdatedestroy và list của cái trên, nếu user là chủ bài viết cũng k đc chỉnh sửa
api tạo group, api xóa group, api liệt kê các group cho user, api thêm member,api xóa member, api tạo post group,api xóa post group, api thêm quyền admin,api accept post,api reject post, api liệt kê post đã đc duyệt và api chưa được duyệt
==================================================================================================================================


sau này triển khai nếu user nằm trong id này thì được xem, user nằm trong id này thì k đc xem(chức năng chỉ hiển thị với người này hoặc ẩn người này của fb)
tạo rules là if user.id mà = với user id được truyền vào thì return True, trong permissions sẽ nhận 1 list user, ở view thì check object permission truyền vào list user
còn ở file post user và post friend chỉ cần lọc ra user_id__in là dcd
===========================================================================================================================

xây group chat và sửa lại tất cả có liên quan
Facebook check theo thứ tự:Có file không? → có → cho qua dù text rỗng
                                          → không → có text không? → có → cho qua
                                                                    → không → chặn

logic tạo group

APi tạo group bao gồm tạo conver và đặt is group =True, sau đó thêm thành viên vào conv_member và đặt người tạo request.user là is_admin luôn

class Conversation(models.Model):
    TYPE_CHOICES = (
        ('direct', 'Direct'),
        ('group', 'Group'),
    )

    id = models.BigAutoField(primary_key=True, editable=False)
    type = models.CharField(max_length=10, choices=TYPE_CHOICES)

    name = models.CharField(max_length=255, null=True, blank=True)  # group dùng
    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)

    status = models.CharField(
        max_length=20,
        choices=(('pending', 'Pending'), ('accept', 'Accept')),
        default='pending'
    )

    def __str__(self):
        return f"Conversation {self.id}"

    class Meta:
        indexes = [
            models.Index(fields=['-updated_at']),
            models.Index(fields=['status']),
        ]
class ConversationMember(models.Model):
    ROLE_CHOICES = (
        ('admin', 'Admin'),
        ('moderator', 'Moderator'),
        ('member', 'Member'),
    )

    conversation = models.ForeignKey(
        Conversation,
        on_delete=models.CASCADE,
        related_name='members'
    )

    user = models.ForeignKey(User, on_delete=models.CASCADE)

    role = models.CharField(
        max_length=20,
        choices=ROLE_CHOICES,
        default='member'
    )

    last_read_message = models.ForeignKey(
        "Message",
        null=True,
        blank=True,
        on_delete=models.SET_NULL
    )

    joined_at = models.DateTimeField(auto_now_add=True)

    def __str__(self):
        return f"{self.user.username} in Conversation {self.conversation.id}"

    class Meta:
        unique_together = ('conversation', 'user')
        indexes = [
            models.Index(fields=['user']),
            models.Index(fields=['conversation']),
            models.Index(fields=['conversation', 'role']),  # 🔥 quan trọng
        ]
api thêm thành viên, api xóa thành viên, api out group sẽ chuyển admin cho user vào thứ 2, api thêm quyền cho user
#==================================================================================================
