work flows
1: tạo trang admin với jazzmin
2: áp dụng silk và debug-toolbar, thử django-extensions, spectacular để test api, tạo file .env lưu tài liệu
3: Tải và xây api RESt,  tạo bảo mật với dj-rest-auth, jwt,allauth, axes[ipware],csp,pwned password -- SAU NÀY DEPLOY ĐỂ DEBUG=FALSE VÀ CHỈNH ALLOWED_HOST VÀ DÙNG DB MYSQL TRÊN PLANET SCALE(FREE), 

	-Bước register: tạo tài khoản, chuyển đến trang html hiển thị gửi mail ,gửi email verified, lưu tài khoản tạm vào db Pending account, nhấn vào email load ra html kèm key fetch tới xác nhận, sau khi xác nhận xóa db Pending account và tạo Profile
	-Bước login: đăng nhập, fetch tới api login, chuyển tới trang chủ
	-Bước reset-password: chuyển tới trang resetpassword, điền email fetch tới password/reset, sau đó gửi mail tới trang html có giao diện điền mật khẩu, post tới reset confirm, load ra trang đã thành công
	-Bước forgot-password: nhấn, chuyển sang trang điền mail, điền mail xong sẽ post mail đó tới api, api tạo ra uid và token gửi kèm mail, mail sẽ gửi link có trang tự tạo để điền mk mới kèm uid và token, sau đó post mk mới,uid,token backend sẽ xác nhận và đổi 
	-Bước login google: nhấn vào login google, load tới trang https://account của gg có chứa google id và callback, sau đó load tới trang html call back tự tạo kèm theo đó là code, sau đó lấy code từ trang call back post tới google login(lưu ý khi đăng nhập gmail đã tồn tại thì gộp lại làm 1), sau khi backend post tới google qua adapter_class thì trả về access token và ta tự tạo refresh
	    Ở Login vào google sẽ check 2 điều kiện là đã login trc đó có bảng sociallogin và chưa login trc đó
	        +Có bảng sociallogin, sẽ đối chiếu user.email lấy từ sociallogin có = với email đang đăng nhập,không bằng là lỗi
	        +Không bảng sociallogin(lần đầu): kiểm tra email có verified, nếu có verified và lấy user.email từ bảng email address và bằng với email đang login google thì kết nối, không là phụ
	-Bước accessToken và refreshToken: viết hàm fetch tới dùng refresh để đổi access sau đó lưu vào local, xây hàm để thông qua nó khi post put máy sẽ biết access hết hạn chưa và làm mới , hàm check login sẽ redirect về trang chủ nếu có access và refresh token
	-Tạo file cookie để set vào cookie refresh token
	-Bước thêm email:
	    Nếu chỉ có google login: Làm mất thì recover ở google
	    Nếu chỉ có password k có gg login :có thể login qua gg để truy cập hoặc dùng password
	    Nếu chỉ có password: mất thì recover qua tin nhắn gmail của backend
	    Khi xóa email phụ nếu chỉ có google: xóa được k cần nhập mật khẩu
	    Khi xóa email phụ nếu có password và gg: bắt buộc nhập password
	    Luồng:
	        Thêm email: gửi mail xác nhận, có thì mới hiện ở tất cả mail user
	        Set email primary: Gửi otp email cũ và nhập mật khẩu
	        Xóa email(chỉ được only mail thường verified): Dùng mật khẩu
	        Xóa Account: Nhập mk và gửi mail otp
4: Sau khi xây Authenticate thì xây serializers
5. phân trang
	data.next để lấy ra trang tiếp theo, phân các dữ liêu lớn thành nhỏ
6. Dùng django-reactions xây like
	tạo react.py có url riêng là create, khi ng dùng post lên thì lấy cái react, lọc ra post đó nếu đã thả và react trùng cũ thì xóa, nếu react mới thì update, nếu chưa có thì tạo. Kèm theo là tạo thêm user_reaction lưu lại lịch sử. Mỗi post có 1 bản ghi đếm là reaction, trả response count,status,type. khi nhấn react ở post sẽ post lên và trả về response cái react đó, fe render ra
7. Xây frontend load ra post, like,...
	Khi ng dùng nhấn, nó sẽ post tới url create kèm type và post id, post id lấy từ dữ liệu được render ra và khi nhấn nút post nào nó lấy id post đó
8. thực hiện like post, gán cảm xúc sau khi f5 vẫn giữ, load ra người like, tạo url láy về người like
9. Thêm ảnh nhiều: nhớ tạo 1 model photo(1-n với post), logic  khi ng dùng thêm vào post và ảnh, nó lấy title ra tạo ra post trước, sau đó lọc ảnh ra thêm vào form data và gửi tới view, view listcreateview gọi serializer của postphoto để create từng ấy ảnh, parser_classes = [MultiPartParser, FormParser] để láy ra dạng formdata, sau đó cuối cùng perform create gán cái id của từng ấy ảnh vào post id
10. Thực hiện chức năng xóa bài viết
11. Thêm thư viện soft delete. Chỉ cần tải thư viện sau đó vào admin thay đổi, sau đó vào admin.py thêm action để lọc ra các deleted và xóa
12.Tạo chức năng sửa bài viết, đi kèm là tạo endpoint mới xóa ảnh theo id để thực thi chức năng xóa ảnh trong chỉnh sửa post. Logic là: đầu tiên nhán chỉnh sửa post nào thì lấy id post đó ra gửi tới endpoint(1) load ra title và ảnh hiện tại, người dùng nhập vào title thì sẽ update ở endpoint(2), còn muốn xóa ảnh thì khi load ra ở endpoint(1) sẽ có các ảnh với id tương ứng và muốn xóa ảnh thì truyền id ảnh đó vào endpoint(3) để xóa
13. thêm thư viện django-activity-stream để tạo log(dùng làm lịch sử hoạt động). Đầu tiên tải thư viện, sau đó thêm vào urls, sau đó đăng kí vào apps.py và cuối cùng chạy signal để tự động chạy. Chức năng nâng cao dùng các hàm helper để lấy hoạt động liên quan tới post và user(thêm vào serializers.py) sau đó xây view
14. Xây thêm 1 model Log để lấy cái meta data trong action, dùng signal tạo ra
15. dùng thư viện django-friendship để xây follow, xây serializer từ model của nó, xây views dựa trên serializer, sau đó xây signal. Sửa trong thư viện gốc cái xử lý lọc reject chưa trong gửi lời mời kết bạn, thêm nữa đưa 1 model nào đó lên đầu, ở cancel đem delete xuống dưới
16. Sửa lại logic của đăng nhập, đăng nhập gg thì sẽ tạo profile và xóa pending profile, tạo tk thì tạo pending profile, thêm extra_data từ goole cung cấp vào profile
17. xây dựng web socket và channels: logic: nhấn chat-> gọi POST server tạo conversation id nếu chưa có, có thì lấy ra, trả về conversation_id-> mở routing thông qua dùng websocket-> kích hoạt consumer để chat 
18. Request message, lúc start conversation nếu là friend thì status là accept còn k thì là pending, xây 2 api accept và reject message request để thay đổi status
19. Cài redis để chạy realtime server, luồng là khi người dùng khi gửi tin nhắn sẽ lưu vào db sau đó gửi tín hiệu và gửi json đến fe. Seen và update cũng tương tự, khi update và seen sẽ gửi đến backend, backend cập nhật db và gửi tín hiệu và json cho fe
20. Tối ưu db
21 Tự viết thêm chức năng đổi email 
22. Ghi log lại tất cả 
23. Xây chức năng notification in-app và push notification qua firebase
24. Xây middleware cho chức năng khi đăng nhập hiện onl. Flow là ng dùng gọi api sẽ qua middleware, middleware custome cho Online logic khi ng dùng onl thì set cache redis thời hạn 5p và nếu gọi liên tục thì refresh lại thgian. Sau đó khi gọi api liên quan đến conversation sẽ thêm is_online lấy ra từ redis cache
25. Thêm friend suggest và search history và thêm log đăng nhạpa đăng xuất dùng ipware
26. Xây update và seen real time và thêm chart admin
27. thay đổi địa chỉ lưu ảnh thành cloud cloudinary
28. xây api search dùng elastic, nhớ dùng
    python manage.py search_index --rebuild
29. chuyển sang db percona trên docker
30. dùng granian, auditlog ghi log server, dùng orson tối ưu serializer, dùng connection pool
31. Tối ưu lại, bỏ n+1 query trong serializer, thay vì gọi từng object thì query hết 1 lượt rồi gọi serializer  và cũng sửa gọi 1 lần get_online của profile
32. Tối ưu trang admin
33. thêm setting tối ưu
34. Sửa lại comment thêm pin comment(sắp xếp comment list trong post theo pin trước và created sau, tạo api thêm pin và xóa pin chung 1 api) thêm nested
35. Tag tên người dùng trong comment: khi người dùng nhấn @, fe thực hiện tìm kiếm với api gọi tất cả profile bạn bè api/friends/, sau đó nhấn chọn người nào sẽ láy id truyền vào cái tạo comment. Khi người dùng nhấn reply 1 reply con, reply con sẽ chứa data parent và data user id, khi gọi api sẽ truyền parent gốc và tag user là user.id
36. Sửa lại Block,Friend,Follow dùng value list cho nhẹ, improve Search API xin như google
37. Thực hiện thêm notification realtime. Khi người dùng vào app sẽ kết nối ws cho notification, khi có thông báo mới sẽ kích hoạt signal và broadcast qua ws tăng count. Khi gọi api mark read sẽ broadcast đặt count là 0,khi vào trang noti thì cũng sẽ nhận thông báo real time qua ws
38 Thực hiện thêm xóa conversation 1 phía. Flow là khi user nhấn xóa sẽ đặt id của field delete_message = với id message mới nhất. Khi list message ra chỉ filter ra các message id ở sau cái delete message
    Flow của ẩn conv khỏi list khi xóa là khi user gọi api xóa sẽ set is_hidden =True, nếu nhắn lần nữa thì set lại là False
39.Thêm reply message. khi user truyền vào id reply sẽ lưu id reply, lấy ra message từ id đó và trả ra cả id lẫn content
40.thêm conversation real time để và broad cast từ chat consumer, chức năng là có tin nhắn mới ở trang conversation thì nhảy lên đầu và chạy dựa vào chat consumer khi groupsend. Có các thông tin như conversation id, last message = message vừa tạo đó để hiện ra chứ k hiện count, updated_at
41. thêm quyền cho post dùng django rules, tạo bảng cho model nằm trong 3 cái public, friends, only. Các api lấy ra feed hay post trang cá nhân user sẽ check nếu is friend thì lấy ra public và friends, nếu là chủ page thì lấy ra only
42. Xây dựng share, thêm model share, api share của 1 bài post, api share của user, api share của bạn bè, api đổi privacy share và api xóa share
    Luồn là luôn check từ post gốc tới post share, post gốc mà bị riêng tư thì k đc load ra, post share mà bị riêng tư cũng k đc load ra, và chỉ đc share bài public hoặc friend
43. Thêm chức năng thêm ảnh vào chat
    (fe) flow là khi gửi ảnh và message gọi api ->
    (be) api post lên cloudinary, cloudinary trả về backend url mã hóa, lưu trước file và url vào db messageattachment tránh mất khi mất connect ws và trả ra data vừa lưu->
    (fe) client nhận về tên file và url và id file lưu, gọi ws truyền cả message và id của file ->
    (be) tạo,lưu message và update lại messageattachment gắn message vào
    *không có message thì chỉ thực thi save file và message là none
    *không có file thì k thực thi lệnh if của file
44. dùng structlog để ghi lại request
45. dùng magic-bin để validate file khi nhận vào tránh file virus.exe mà đổi thành virus.png(vẫn bị bypass)
46. Thêm preview card. Flow là khi người dùng nhấn share và paste vô messenger thì bot của Fb sẽ get html từ link(api ở be) về để load ra preview. Khi user ấn vào link(api ở be) nó sẽ check k phải bot thì redirect frontend và frontend gọi api load ra json bài viết, sau này đặt share là 1 domain riêng và đổi share.socialnetwork.com
47. thêm xóa all và xóa từng search history, thêm pin post và thêm report post
48.  thêm typing realtim, ng dùng nhập thì gọi ws type= typing
49. Tách phone và birth ra khỏi response trả về vì bảo mật. Thêm vào profile 2 field là có đc public birth và phone, nếu được thì response serializer sẽ check và return không thì None. Thêm API để enable 2 field đó và sửa phone/birth
50. Thêm khi update ảnh lần đầu thì is_complete=True, còn False thì hiện thông báo ở fe cho mn biết
51. THêm support ticket user gửi yêu cầu và nhân viên sẽ hỗ trợ reply qua mail
52. Sửa lại email phụ không đăng nhập được google, chỉ email chính
53. Thêm celery chạy các task ngầm như push notification qua firebase và sendmail, xóa ảnh softdelete theo n thời gian
54. Thêm validator cho first_name, last_name, ngày sinh số điện thoại cho model và RegisterSerializer
55. Khi vào profile mà is_completed =False thì hiện box báo là hãy cập nhật ảnh. Khi user update mà có ảnh thì đổi is_complete=True
56. Mã hóa tin nhắn trong database dùng from encrypted_fields.fields import EncryptedTextField
57. Thêm api xóa notification
58 deploy và đẩy project thành container trong docker
59. Thêm group chat: flow là nhấn chat với ai đó thì gọi startchat, còn nhấn tạo group sẽ gọi api tạo group
    creategroup khi nhấn tạo sẽ gọi api/friends và user chọn ai thì truyền vào đoạn chat
    đổi admin: khi nhán sẽ gọi api check có phải admin k và đổi
    thêm thành viên: gọi api/user/friends?exclude_group_id=... lấy ra bạn bè chưa có trong group, sau đó gọi api check user có là thành viên, id thêm = id truyền vào - id đã tồn tại
    modify group chat đổi tên và ảnh
    kickmember: check xem có là admin không, khi kick gọi api lấy ra danh sách thành viên mà kick
    DeleteGroupChat: yêu cầu nhập mật khẩu
    LeaveGroupChat: gọi api lấy thành viên và trao quyền cho user đó mới đc rời
=====================================================================================