
định nghĩa trước:

logic: khi kết bạn nhất chat sẽ tạo conversation, gọi api startconversation, không gán tên, không gán avatar, is_group=false
	khi nhấn tạo chat, gọi api creategroup, gán tên(bắt buộc) và avatar(thêm default là ảnh), is_group=true, thêm thành viên
	frontend khi thấy conversation là is_group=false thì sẽ gán tên của đối phương cho nhau, gán href id vào và khi nhán vào mở đoạn chat
	khi ấn ok ở request sẽ tạo conversation








from django.contrib.auth import get_user_model
from rest_framework_simplejwt.tokens import RefreshToken

User = get_user_model()
user = User.objects.get(email='trannghi16724@gmail.com')  # thay email thật vào
token = RefreshToken.for_user(user)
print(f"Access: {str(token.access_token)}")
print(f"Refresh: {str(token)}")

